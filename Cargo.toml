[package]
name = "ezlog"
version = "0.1.0"
edition = "2024"
license = "AGPL-3.0-or-later"
description = "Lightly configured env_logger with colored and simpler log formatting"

[dependencies]
env_logger = "0.11.8"
log = "0.4.27"
time = { version = "0.3.41", features = ["local-offset"] }
